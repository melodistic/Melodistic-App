import 'package:flutter/material.dart';

class SettingPreferenceScreen extends StatelessWidget {
  const SettingPreferenceScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Setting Preference Screen'),
    );
  }
}